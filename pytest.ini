[pytest]
addopts = -ra -q --alluredir=allure-results
python_files = *_tests.py
testpaths = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    known_bug: ожидаемый провал до исправления стенда (см. BUGS.md); используется вместе с xfail
